apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: reports
  name: reports
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: grpc
      port: 50051
      targetPort: grpc
  selector:
    app.kubernetes.io/name: reports
